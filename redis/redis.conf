# Bind to all interfaces (Docker network)
bind 0.0.0.0

# Default Redis port
port 6379

# Enable protected mode
protected-mode no

# Disable persistence (cache-only usage)
save ""

# Log level
loglevel notice
